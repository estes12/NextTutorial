import DashboardSkeleton from '@/app/ui/skeletons';

export default function Loading() {
  return <DashboardSkeleton/>;
}

//file name in parenthesis does not effect url. /dashboard/(overview) just reads as /dashboard